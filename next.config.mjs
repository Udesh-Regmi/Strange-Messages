/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['utfs.io','scontent-atl3-1.xx.fbcdn.net','images.unsplash.com'], // Add the required domain here
      },
};

export default nextConfig;
